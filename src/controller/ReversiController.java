package controller;

import interfaces.Playable;

public class ReversiController extends AbstractController implements Playable {
    private service.ServerService serverService;

    @Override
    public String makeMove() {
        return null;
    }
}
