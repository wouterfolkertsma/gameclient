package controller;

import interfaces.Playable;

public class TicTacToeController extends AbstractController implements Playable {
    private service.ServerService serverService;

    @Override
    public String makeMove() {
        return null;
    }
}
